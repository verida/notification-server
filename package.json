{
  "name": "@verida/notification-server",
  "version": "0.1.0",
  "description": "Verida notification server",
  "main": "dist/server.js",
  "scripts": {
    "dev": "nodemon",
    "build": "rm -rf dist && tsc",
    "prestart": "yarn build",
    "start": "node dist/server.js",
    "tests": "ts-mocha './test/**/*.ts'",
    "test": "ts-mocha",
    "lambda-deploy": "rm -rf dist && tsc && yarn claudia create --region us-east-2 --handler lambda.handler --deploy-proxy-api --no-optional-dependencies --set-env-from-json .env.prod.json --config claudia.json",
    "lambda-update": "rm -rf dist && tsc && yarn claudia update --set-env-from-json .env.prod.json --config claudia.json",
    "acacia-lambda-deploy": "rm -rf dist && tsc && yarn claudia create --region us-east-2 --handler lambda.handler --deploy-proxy-api --no-optional-dependencies --set-env-from-json .env.acacia.json --config claudia.acacia.json",
    "acacia-lambda-update": "rm -rf dist && tsc && yarn claudia update --set-env-from-json .env.acacia.json --config claudia.acacia.json",
    "lambda-pack": "rm -rf dist && tsc && yarn claudia pack --no-optional-dependencies"
  },
  "files": [
    "**/*.ts",
    "**/*.js",
    "**/*.mjs",
    "**/*.cjs",
    "**/*.json",
    "**/*.svg",
    "**/*.xml",
    "**/*.yml",
    "**/*.lock",
    "**/*.bnf",
    "**/*.map",
    ".nvmrc"
  ],
  "repository": "https://github.com/verida/notification-server",
  "author": "Chris <chris@verida.io>",
  "license": "ISC",
  "private": true,
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/mocha": "^9.1.0",
    "claudia": "^5.14.0",
    "mocha": "^9.1.3",
    "nodemon": "^2.0.14",
    "ts-mocha": "^8.0.0",
    "typescript": "^4.4.4"
  },
  "dependencies": {
    "@verida/account-node": "^2.0.0",
    "@verida/client-ts": "^2.0.1",
    "@verida/did-client": "^2.0.0",
    "@verida/encryption-utils": "^2.0.0",
    "aws-serverless-express": "^3.4.0",
    "axios": "^0.24.0",
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "express-basic-auth": "git+https://github.com/Mozzler/express-basic-auth.git",
    "firebase-admin": "^10.0.0",
    "memory-cache": "^0.2.0",
    "nano": "^9.0.5"
  }
}
